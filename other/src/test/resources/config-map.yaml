apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: default
  labels:
    app: mn-kc-test-config-map
data:
  app-config.yml: |-
    greeting:
      message: Hello %s from a ConfigMap!
    thorntail:
      logging: INFOs
